// Программа, которая читаем в формате JSON и возвращает их в XML и наоборот.
package main

func main() {
	// Получаем путь к файлу из аргументов командной строки
	//filepathForOld, filepathForNew, err := utils2.ParseFileForCompareDB()
	//utils2.HandleError(err)
	//
	//// Создаем ридер на основе типа файла (JSON или XML)
	//reader, _, err := dbreader.NewDBReader(filepathForOld)
	//utils2.HandleError(err)
	//
	//// Читаем содержимое файла
	//dataOld, err := utils2.ReadFile(filepathForOld)
	//utils2.HandleError(err)
	//
	//// Десериализуем данные в структуру
	//recipeOld, err := reader.Read(dataOld)
	//utils2.HandleError(err)
	//
	//// Создаем ридер на основе типа файла (JSON или XML)
	//reader, _, err = dbreader.NewDBReader(filepathForNew)
	//utils2.HandleError(err)
	//
	//// Читаем содержимое файла
	//dataNew, err := utils2.ReadFile(filepathForNew)
	//utils2.HandleError(err)
	//
	//// Десериализуем данные в структуру
	//recipeNew, err := reader.Read(dataNew)
	//utils2.HandleError(err)

}
